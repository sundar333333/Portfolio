I want to completely replace the "Works" section with a 3D Infinite Circular Carousel, exactly like the reference video I provided.

### 1. The Layout: Cylindrical Carousel
Instead of a flat list, arrange the 4 Project Posters in a circle around the camera (a 3D cylinder layout).
* **Radius:** Place the images at a radius of 3.5 units from the center.
* **Orientation:** Each poster must effectively "look at" the center (0,0,0) so that when it rotates to the front, it faces the user flat-on.
* **Spacing:** Distribute the 4 projects evenly (0, 90, 180, 270 degrees) but allow the carousel to spin infinitely.

### 2. The Interaction: Horizontal Scroll & Drag
* **Scroll Controls:** Map the user's vertical scroll to the **Y-Rotation** of the entire carousel group.
* **Feel:** As the user scrolls down, the carousel spins horizontally. It should feel smooth and weighty (use `drei`'s `ScrollControls` with damping).

### 3. The "Gyro" & Parallax Effect
The user explicitly requested a "Gyro" function for the active poster.
* **Mouse Parallax:** Track the mouse position `(x, y)`.
* **Effect:** Apply a slight, subtle rotation to the *individual* poster meshes based on the mouse position.
* **Result:** As the user moves their mouse, the posters should tilt slightly (like a 3D holographic card), independent of the main carousel rotation.

### 4. CRITICAL: Fix HD Image Blur (Compression Issue)
My images are HD, but they look blurry/compressed in the 3D scene. You must fix the texture encoding.
* **Anisotropy:** Set `texture.anisotropy = 16` (max quality).
* **MinFilter:** Set `texture.minFilter = THREE.LinearFilter`.
* **Tone Mapping:** Ensure the materials are not being washed out by lighting. Use `MeshBasicMaterial` or `MeshStandardMaterial` with `toneMapped={false}` to keep the original colors punchy and sharp.

### 5. Project Data (The 4 Items)
Use the 4 posters I uploaded for these items:
1.  Current Mobile Payment
2.  Eventify
3.  Space Jump
4.  Ticking

Please implement this `Carousel3D` component now using React Three Fiber.